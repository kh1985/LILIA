# Three Hook Active Wiring Smoke - 2026-05-10

## Scope

- Task: HOOK-003 smoke, generated Active Hook wiring.
- Session: `smoke_three_hook_20260510`
- Result: PASS for generated session state, downstream document validation, hook wiring, and state consistency.
- Play Mode run output: not verified because local LLM run commands failed before producing playable text.
- WBS status: not changed.

## Commands

```bash
./lilia new smoke_three_hook_20260510 --prompt-only --engine auto
./lilia apply-newgame smoke_three_hook_20260510 saves/smoke_three_hook_20260510/answers.md --engine auto
python -m pytest tests/test_document_generator_prompts.py
python -m pytest tests/test_session_document_validator.py
python -m pytest tests/test_state_consistency.py
python -m pytest
./lilia validate-session smoke_three_hook_20260510
./lilia resume smoke_three_hook_20260510 --run --engine auto
./lilia resume smoke_three_hook_20260510 --run --engine claude
git diff --check
git diff --stat
git status --short
```

## apply-newgame / Downstream Generation

Initial `apply-newgame` run failed in Group A after three attempts.

- character.yaml: generated by `codex`.
- profile.md: generated by `codex`.
- story_spine / relationship_spine: generated by `codex`.
- Group A first run: failed because `story/story_deck.md` and `current/event_card.md` left Three Hook / Active Hook / Scene Function fields empty.

Minimal fix applied:

- `tools/session/document_generator.py` now includes the `### Main Hook`, `### Relationship Hook`, and `### Life-Exploration Hook` scaffold in the prompt structure and explicit Group A instructions.
- `tests/test_document_generator_prompts.py` now checks the H3 hook scaffold appears in the prompt.

Re-run result:

- Group A attempts 1-2 with `codex`: failed because `Active Hook` / `Scene Function` fields were still empty.
- Group A attempt 3 with `claude`: passed.
- Combined downstream validation: PASS.
- Final `apply-newgame`: success, phase `first_scene_ready`.

Log:

- `logs/apply_newgame_smoke_three_hook_20260510_20260510_083305.log`
- `logs/apply_newgame_smoke_three_hook_20260510_20260510_084429.log`

## story_deck Three Hook Spine

`saves/smoke_three_hook_20260510/story/story_deck.md` contains `## Three Hook Spine`.

Generated hook ids:

- Main Hook: `main_stopped_watch_after_hours`
- Relationship Hook: `relationship_keep_counter_distance`
- Life-Exploration Hook: `life_after_closing_route`

Status:

- Main Hook: `active`
- Relationship Hook: `background`
- Life-Exploration Hook: `background`

Checks:

- Main / Relationship / Life fields are filled.
- Life Hook has a non-empty life/movement scope: station-front repair shop area, rainy street, commute route, return home, revisit, waiting nearby.
- Relationship Hook uses distance, waiting, boundary, trust, and naming, not affinity or route tracking.
- No `好感度`, `攻略`, `ルート`, `AFFINITY`, `bond`, `affection meter`, or `romance route` leakage found in generated story_deck / event_card.
- No numbered Three Hook 3-choice UI found in generated story_deck / event_card.

## event_card Active Hook / Scene Function

`saves/smoke_three_hook_20260510/current/event_card.md` contains `## Active Hook`.

Generated Active Hook:

```md
- hook_id: main_stopped_watch_after_hours
- hook_type: main
- status: active
```

`hook_id` matches the Main Hook in `story/story_deck.md`.

`## Scene Function` is present and filled:

- function: `起点`
- current_question: filled
- entry_state: filled
- exit_condition: filled
- change_delta: filled
- next_hook_candidate: filled

Existing Event Card required fields are present and filled:

- `Visible Problem`
- `First Concrete Action`
- `Handles 2-4`
- `Relationship Stake`
- `If Ignored`
- `Next Visible Change`

## Validator Results

Downstream document validator:

```text
document validation: PASS
```

State consistency:

```text
state consistency: PASS
```

Important fix found by smoke:

- `state_consistency.latest_next_hook()` was treating the plural `## Candidate Next Hooks` shelf as a stale next_hook promotion candidate.
- This caused generated initial sessions to fail `validate-session`.
- Minimal fix: only singular `## Candidate Next Hook` is treated as a promotion candidate; plural `## Candidate Next Hooks` remains a material shelf.
- `tests/test_state_consistency.py` updated to cover this.

## Play Mode Leakage Check

Generated state check:

- No management-term leakage found in generated `story_deck.md` / `event_card.md` for the searched terms.
- `Active Hook`, `Scene Function`, `hook_id`, and function labels remain state fields, not Play Mode text.

Play Mode runtime output was not verified:

- `./lilia resume smoke_three_hook_20260510 --run --engine auto` failed before playable output:
  - `codex CLI failed`
  - app-server client initialization: `Operation not permitted`
- `./lilia resume smoke_three_hook_20260510 --run --engine claude` failed before playable output:
  - `claude CLI returned empty output`

Next task should run Play Mode leakage inspection in an environment where the chosen LLM CLI can produce runtime output.

## Test Results

```text
python -m pytest tests/test_document_generator_prompts.py
4 passed

python -m pytest tests/test_session_document_validator.py
10 passed, 1 skipped

python -m pytest tests/test_state_consistency.py
7 passed

python -m pytest
184 passed, 1 skipped
```

## Found Issues

1. Group A generation still needed explicit H3 hook scaffold in the prompt. Fixed.
2. Codex did not fill Active Hook / Scene Function on Group A attempts 1-2 even after prompt reinforcement; Claude fallback passed. Keep this as a model-compliance risk.
3. `validate-session` misread plural `Candidate Next Hooks` as a promotion candidate. Fixed.
4. Runtime Play Mode leakage could not be checked because both local resume run engines failed before producing playable text.

## Next Work

- HOOK-003 follow-up: run `resume --run` leakage check in a working LLM runtime.
- HOOK-004/005: apply-turn hook state updates and promotion integration, without treating `Candidate Next Hooks` plural shelf as active.
- Consider adding a release smoke that asserts generated `event_card` Active Hook and `story_deck` Three Hook IDs match for a real apply-newgame session.
