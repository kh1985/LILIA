# TTY Multi-turn Interactive Save/Resume Smoke - 2026-05-09

## Summary

- Result: partial / blocked.
- Session: `smoke_tty_20260509`.
- True TTY coverage: `./lilia codex-resume smoke_tty_20260509` launched Codex interactive from a PTY.
- Multi-turn play coverage: not confirmed.
- Save / apply-turn / resume after TTY play: not confirmed.
- Production session / saves impact: none. Only ignored test session `saves/smoke_tty_20260509` was created.

This smoke should not be treated as a pass for the remaining TTY multi-turn item. The launcher can enter the interactive Codex path, but this Codex app PTY exposed `TERM=dumb` behavior and the nested TUI did not reach a stable playable multi-turn flow before interruption.

## Adoption / Non-adoption Context

Adopted assumptions:

- MIRA: `core / voice / state / relationship / memory / beliefs` remain separate responsibility areas.
- inner-galge: `hotset` is a resume-temperature helper, not the only source of truth.
- LIRIA: `session`, `event_card`, save/resume, and archive-style state separation are the operating model.

Not adopted for this smoke:

- AI Harness / autoplay.
- Production CI or export flows.
- Editing existing production sessions.
- WBS status changes.

## Preconditions

Read or checked:

- `AGENTS.md`
- `PROJECT_CONTROL.md`
- `STATUS_DASHBOARD.html`
- `docs/RESUME_SMOKE_TEST.md`
- `docs/GROWTH_UPDATE_LOOP.md`
- `docs/VOICE_CONTINUITY.md`
- `docs/EVENT_CARD_PLAYABILITY.md`
- `docs/specs/PLAY_MODE_SPEC.md`
- `prompt/core.md`
- `prompt/save_resume.md`
- `tests/resume_smoke/results/2026-05-09_manual.md`
- `tests/resume_smoke/results/2026-05-09_autosave.md`
- `./lilia` around `codex-resume`, `resume`, `scene-tick`, and `apply-turn`

Skill note:

- Tried `python scripts/liria_prompt_auditor.py --root .` because the prompt-auditor skill applies to prompt/session hygiene checks.
- Result: not available in this repository (`No such file or directory`).

## Session Setup

Command:

```sh
cp -R saves/smoke_autosave_20260509 saves/smoke_tty_20260509
perl -0pi -e 's/smoke_autosave_20260509/smoke_tty_20260509/g' saves/smoke_tty_20260509/session.json
python -m json.tool saves/smoke_tty_20260509/session.json | sed -n '1,140p'
```

Initial state:

- `current_phase`: `active`
- `initialization.first_scene_status`: `started`
- `autosave.turns_since_save`: `0`
- `autosave.autosave_required`: `false`

Reason:

- The prior `smoke_autosave_20260509` session already represents a dedicated ignored smoke session with `active` phase after `apply-turn`.
- Copying it avoids touching production saves and starts from the desired post-save resume state.

## TTY Launch Attempt

Command was run in a PTY:

```sh
./lilia codex-resume smoke_tty_20260509
```

Observed:

- `./lilia` printed the Codex interactive command and wrote `/tmp/lilia_smoke_tty_20260509_resume_prompt.md`.
- The launcher detected a TTY and attempted the real interactive path instead of the fallback.
- Codex CLI launched and showed:
  - `WARNING: TERM is set to "dumb". Codex's interactive TUI may not work in this terminal.`
  - `Continue anyway? [y/N]:`
- Entered `y` and Codex interactive started.
- The nested Codex session read the resume prompt and session files, then entered long exploration.
- The TUI output was noisy and difficult to operate reliably in the Codex app PTY.
- No stable playable scene response was captured.
- No player turn was entered.
- The nested session was interrupted with Escape and then exited.

True TTY boundary:

- Confirmed: `codex-resume` sees a TTY and starts Codex interactive.
- Not confirmed: stable player input, multiple normal turns, natural Save Mode transition, or resume-after-save from inside the nested interactive session.

## Resume Bundle Check

Command:

```sh
./lilia resume smoke_tty_20260509 --prompt-only | sed -n '1,40p'
```

Observed:

- Resume bundle says:
  - `profile: omitted by default; read lilia/main/profile.md only for voice/personality drift or initial-core checks`
- This matches the expected `active` / `first_scene_status: started` path.
- `profile.md` is still referenced in general rules as an optional fallback, but it is not included as a default resume file.

Session state check:

```sh
rg -n "profile:|profile.md|current_phase|first_scene_status|autosave_required|turns_since_save" \
  /tmp/lilia_smoke_tty_20260509_resume_prompt.md \
  saves/smoke_tty_20260509/session.json
```

Observed:

- `current_phase`: `active`
- `first_scene_status`: `started`
- `turns_since_save`: `0`
- `autosave_required`: `false`

## Event Card Playable Check

Command:

```sh
for h in 'Visible Problem' 'First Concrete Action' 'Handles 2-4' 'Relationship Stake' 'If Ignored' 'Next Visible Change' 'Next Hook'; do
  if rg -q "^## ${h}$|^## ${h} -" saves/smoke_tty_20260509/current/event_card.md; then
    echo "OK $h"
  else
    echo "MISSING $h"
  fi
done
```

Observed:

- `OK Visible Problem`
- `OK First Concrete Action`
- `OK Handles 2-4`
- `OK Relationship Stake`
- `OK If Ignored`
- `OK Next Visible Change`
- `OK Next Hook`

## Voice / Relationship / Memory Preflight

Command:

```sh
./lilia resume smoke_tty_20260509 | sed -n '1,80p'
```

Observed:

- No voice-continuity warning text was printed.
- The command returned the expected prompt/run hints.

Additional read-only inspection showed:

- `hotset.md` has a next first-turn temperature:
  - 澪 remembers that `かねこさん` waited without touching the photo.
  - Next entry is the unresolved handling of the unnamed photo.
- `scene.md` keeps the current place, distance, prior exchange, and next user agency.
- `relationship.md`, `memory.md`, and `beliefs.md` contain the same trust/boundary beat outside hotset, so hotset is not the only source of truth.

## Scene Tick Result

Not run for this TTY attempt.

Reason:

- No normal playable turn was completed in the nested TTY session.
- Running `scene-tick` without a completed normal turn would make the counter look healthier than the actual TTY evidence.

## Save Mode / Apply-turn Result

Not run for this TTY attempt.

Reason:

- The smoke did not reach a real multi-turn play interval or an explicit Save Mode request inside the nested TTY session.
- The prior autosave smoke already confirmed counter reset after `apply-turn`; this file is only for the remaining TTY path.

## Resume Result

Confirmed only at prompt-bundle level:

- Resume prompt uses lightweight order.
- `profile.md` is omitted by default for the copied `active` session.
- `event_card`, `hotset`, `scene`, `relationship`, `memory`, and `beliefs` are present and consistent enough for a next entry.

Not confirmed:

- Generated resume first voice from a true TTY play loop.
- First post-save player-facing line after `apply-turn`.
- Natural continuation from the next hook after a TTY Save Mode update.

## Arc Closure / Scene Progression Observation

Not confirmed in generated TTY play text.

Preflight state is compatible:

- `event_card.md` contains a next hook that can move from the closed first exchange into the next small entrance.
- `scene.md` has a concrete next action choice instead of only lingering atmosphere.

Still unverified:

- Whether the interactive GM actually avoids repeating the same afterglow motif across multiple TTY turns.
- Whether the GM closes the scene naturally after the core beat and leaves one next entrance.

## Issues Found

1. The in-app PTY can launch nested Codex interactive, but `TERM=dumb` makes the TUI unreliable for this smoke.
2. The nested Codex session began long workspace exploration and did not reach playable response quickly enough for a controlled smoke.
3. The true TTY multi-turn save/resume path remains unconfirmed.
4. The resume bundle still contains both older `prompt/core.md` autosave wording and newer `prompt/save_resume.md` / CLI autosave wording. For this smoke, `prompt/save_resume.md` and CLI behavior remain the operative source, but the wording mismatch is still visible in the bundle.

## Next Tasks

1. Re-run the TTY smoke from a real external terminal with a normal terminal type, for example `TERM=xterm-256color`.
2. In that run, complete at least 3 normal player turns, with `./lilia scene-tick smoke_tty_20260509` after each turn.
3. Then trigger an explicit save or continue until `autosave_required: true`, write a minimal `turn_update.md`, run `./lilia apply-turn smoke_tty_20260509 <turn_update.md>`, and resume from `./lilia codex-resume smoke_tty_20260509`.
4. If external-terminal TTY remains hard to drive, consider a separate small task for a smoke-friendly interactive driver or clearer launcher fallback, without changing Play Mode / Save Mode semantics.
5. Consider a separate docs/prompt cleanup task to reconcile the older autosave wording in `prompt/core.md` with `prompt/save_resume.md`.

## Final Status

- TTY launch path: partial pass.
- Multi-turn play: not confirmed.
- Save Mode in TTY path: not confirmed.
- `apply-turn` after TTY play: not confirmed.
- Resume after TTY save: not confirmed.
- `first_scene_ready` / active state: active and started before/after attempt.
- Production saves: not touched.
- WBS status: not changed.
